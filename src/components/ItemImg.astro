---
const { id, img, title } = Astro.props;
---

<a class="overflow-hidden w-[445px] rounded-lg" href={`/${id}`}>
  <img
    style={`view-transition-name: image-${id};`}
    class="object-cover aspect-video rounded-lg transition-transform hover:scale-110 max-w-[445px]"
    src={img}
    alt={title}
  />
</a>
