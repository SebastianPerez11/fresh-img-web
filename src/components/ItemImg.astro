---
const { id, img, title } = Astro.props;
---

<a class="overflow-hidden rounded-lg min-w-[340px]" href={`/${id}`}>
  <img
    style={`view-transition-name: image-${id};`}
    class="w-full object-cover aspect-video rounded-lg transition-transform hover:scale-110"
    src={img}
    alt={title}
  />
</a>
<!-- min-sm:max-w-72 max-2xl:max-w-[445px]  -->
