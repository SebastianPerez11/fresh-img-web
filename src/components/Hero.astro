---
const { subtitle, tag} = Astro.props;
---

<div class="w-full max-w-[1920px] flex flex-col gap-5">
    <header class="w-full flex flex-col gap-1">
        <h1 class="text-3xl font-semibold">Encontra los mejores <span class="text-cyan-500">Wallpapers</span> de tus tecnologias favoritas</h1>
        <p class="text-xl">{subtitle}</p>
    </header>
    <footer class="w-full">
        <a href="/" class="bg-slate-200 rounded-full px-4 py-1">{tag}</a>
    </footer>
</div>